@model IEnumerable<d20.InitTracker.Data.Models.Encounter>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EncounterName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EncounterStartDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EncounterName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EncounterStartDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EncounterKey">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EncounterKey">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EncounterKey">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
